<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_dark"
    android:paddingTop="32dp">

    <!-- Header -->
    <ImageButton
        android:id="@+id/btnBackDetalleAlumno"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginStart="16dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_arrow_back"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="@color/accent_green" />

    <TextView
        android:id="@+id/tvNombreAlumnoDetalle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="16dp"
        android:text="Detalle del Alumno"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@id/btnBackDetalleAlumno"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/btnBackDetalleAlumno"
        app:layout_constraintTop_toTopOf="@id/btnBackDetalleAlumno" />

    <!-- Tabs -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tabLayoutAlumno"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:background="@color/background_dark"
        app:layout_constraintTop_toBottomOf="@id/btnBackDetalleAlumno"
        app:tabIndicatorColor="@color/accent_green"
        app:tabSelectedTextColor="@color/accent_green"
        app:tabTextColor="@color/text_secondary">

        <com.google.android.material.tabs.TabItem
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Activas" />

        <com.google.android.material.tabs.TabItem
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Completadas" />
    </com.google.android.material.tabs.TabLayout>

    <!-- RecyclerView for Routines -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvRutinasDetalleAlumno"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:padding="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tabLayoutAlumno"
        tools:listitem="@layout/item_routine" />

    <TextView
        android:id="@+id/tvEmptyRutinas"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="No hay rutinas en esta sección"
        android:textColor="@color/text_hint"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tabLayoutAlumno" />

</androidx.constraintlayout.widget.ConstraintLayout>
