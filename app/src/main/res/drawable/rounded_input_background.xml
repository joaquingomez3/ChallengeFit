<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fondo del Input -->
    <solid android:color="#1A2A3A" /> <!-- Azul marino un poco más claro -->

    <!-- Bordes redondeados -->
    <corners android:radius="10dp" />

    <!-- Borde suave -->
    <stroke
        android:width="1dp"
        android:color="#2F4A5F" />

    <!-- Padding interno (opcional, pero recomendable) -->
    <padding
        android:left="10dp"
        android:top="8dp"
        android:right="10dp"
        android:bottom="8dp" />
</shape>
